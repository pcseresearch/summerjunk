#include <ostream>

void find_gauss_mean()
{
	FILE *freader = fopen("0001_0_1.txt", "r");

	DataGridView grid = new DataGridView();
	for(i=0;i<11;i++)
	{
		if(i!=6 || i!=10)
		grid.Columns[i].Visible = false;
		else
		printfreader("%s");
	}



}
